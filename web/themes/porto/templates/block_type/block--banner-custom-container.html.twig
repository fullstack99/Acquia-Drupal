{%
	set classes = [
	'block',
	'block-' ~ configuration.provider|clean_class,
	'block-' ~ plugin_id|clean_class,
	]

	%}
	<div{{ attributes.addClass(classes) }}>
	{{ title_prefix }}
	<div class="banners-container">
		<div class="container">
			<div class="row">
				{{content.field_banner_container_hp}}
			</div>
		</div>
	</div>
	{{ title_suffix }}
</div>